ENVIRONMENT=development
PORT=8000
MONGODB_URL=mongodb://127.0.0.1:27017/dowells

JWT_ACCESS_TOKEN_SECRET=
JWT_REFRESH_TOKEN_SECRET=

FRONT_END_URL=http://localhost:3000

AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=

# Used only by npm run seed:admin (there is no self-registration endpoint).
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=
ADMIN_SEED_NAME=

# Google OAuth — code exists (src/controllers/user) but is not mounted in
# any router yet; these are unused until a future phase wires it up.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URL=
